# F1 Fantasy League
A Streamlit app for managing F1 fantasy predictions and leaderboards.

## Setup
1. Clone the repo
2. Install dependencies: `pip install streamlit`
3. Run with: `streamlit run app.py`
